The Language CppGrammar
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of CppGrammar==

===Literals===
Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




Id literals are recognized by the regular expression
`````letter ('_' | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in CppGrammar are the following:
  | ``bool`` | ``const`` | ``do`` | ``double``
  | ``else`` | ``false`` | ``for`` | ``if``
  | ``inline`` | ``int`` | ``return`` | ``string``
  | ``struct`` | ``throw`` | ``true`` | ``typedef``
  | ``using`` | ``void`` | ``while`` |

The symbols used in CppGrammar are the following:
  | { | } | ; | (
  | ) | , | [ | ]
  | = | . | -> | ++
  | -- | * | ! | /
  | % | + | - | <<
  | >> | < | > | <=
  | >= | == | != | &&
  | || | += | -= | ?
  | : | :: | [] | &

===Comments===
Single-line comments begin with #, //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of CppGrammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Def]//
  | //Body// | -> | ``{`` //[Stm]// ``}``
  |  |  **|**  | ``;``
  | //Def// | -> | //Decl// ``;``
  |  |  **|**  | //Type// //Id// ``(`` //[Arg]// ``)`` //Body//
  |  |  **|**  | ``inline`` //Type// //Id// ``(`` //[Arg]// ``)`` //Body//
  |  |  **|**  | ``using`` //Type// ``;``
  |  |  **|**  | //TypeDef//
  |  |  **|**  | //StructDef// ``;``
  | //[Def]// | -> | **eps**
  |  |  **|**  | //Def// //[Def]//
  | //Decl// | -> | //Type// //[IdSpec]// //MaybeAss//
  | //StructDef// | -> | ``struct`` //Id// ``{`` //[Decl]// ``}``
  | //TypeDef// | -> | ``typedef`` //Type// //Id// ``;``
  |  |  **|**  | ``typedef`` //StructDef// //Id// ``;``
  | //[Decl]// | -> | **eps**
  |  |  **|**  | //Decl// ``;`` //[Decl]//
  | //Arg// | -> | //Type//
  |  |  **|**  | //Type// //Id//
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg//
  |  |  **|**  | //Arg// ``,`` //[Arg]//
  | //IdSpec// | -> | //Id//
  |  |  **|**  | //Id// ``[`` //Integer// ``]``
  | //[IdSpec]// | -> | //IdSpec//
  |  |  **|**  | //IdSpec// ``,`` //[IdSpec]//
  | //Stm// | -> | ``;``
  |  |  **|**  | //Exp// ``;``
  |  |  **|**  | //Decl// ``;``
  |  |  **|**  | //Type// //Id// ``=`` //Exp// ``;``
  |  |  **|**  | ``return`` //Exp// ``;``
  |  |  **|**  | ``return`` ``;``
  |  |  **|**  | ``return`` //Exp// ``;``
  |  |  **|**  | ``while`` ``(`` //Exp// ``)`` //Stm//
  |  |  **|**  | ``do`` //Stm// ``while`` ``(`` //Exp// ``)``
  |  |  **|**  | ``for`` ``(`` //Decl// ``;`` //Exp// ``;`` //Exp// ``)`` //Stm//
  |  |  **|**  | ``for`` ``(`` //Exp3// ``=`` //Exp2// ``;`` //Exp// ``;`` //Exp// ``)`` //Stm//
  |  |  **|**  | ``{`` //[Stm]// ``}``
  |  |  **|**  | //TypeDef//
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //Stm//
  |  |  **|**  | ``if`` ``(`` //Exp// ``)`` //Stm// ``else`` //Stm//
  |  |  **|**  | //StructDef// ``;``
  | //[Stm]// | -> | **eps**
  |  |  **|**  | //Stm// //[Stm]//
  | //Exp16// | -> | ``true``
  |  |  **|**  | ``false``
  |  |  **|**  | //Integer//
  |  |  **|**  | //Double//
  |  |  **|**  | //Char//
  |  |  **|**  | //[ConstantName]//
  |  |  **|**  | //[String]//
  |  |  **|**  | ``(`` //Exp// ``)``
  | //[String]// | -> | //String//
  |  |  **|**  | //String// //[String]//
  | //Exp15// | -> | //Exp15// ``[`` //Exp// ``]``
  |  |  **|**  | //Exp15// ``(`` //[Exp]// ``)``
  |  |  **|**  | //Exp16//
  | //Exp14// | -> | //Exp14// ``.`` //Exp15//
  |  |  **|**  | //Exp14// ``->`` //Exp15//
  |  |  **|**  | //Exp15// ``++``
  |  |  **|**  | //Exp15// ``--``
  |  |  **|**  | ``*`` //Exp15//
  |  |  **|**  | //Exp15//
  | //Exp13// | -> | ``++`` //Exp14//
  |  |  **|**  | ``--`` //Exp14//
  |  |  **|**  | ``!`` //Exp14//
  |  |  **|**  | //Exp14//
  | //Exp12// | -> | //Exp12// ``*`` //Exp13//
  |  |  **|**  | //Exp12// ``/`` //Exp13//
  |  |  **|**  | //Exp12// ``%`` //Exp13//
  |  |  **|**  | //Exp13//
  | //Exp11// | -> | //Exp11// ``+`` //Exp12//
  |  |  **|**  | //Exp11// ``-`` //Exp12//
  |  |  **|**  | //Exp12//
  | //Exp10// | -> | //Exp10// ``<<`` //Exp11//
  |  |  **|**  | //Exp10// ``>>`` //Exp11//
  |  |  **|**  | //Exp11//
  | //Exp9// | -> | //Exp9// ``<`` //Exp10//
  |  |  **|**  | //Exp9// ``>`` //Exp10//
  |  |  **|**  | //Exp9// ``<=`` //Exp10//
  |  |  **|**  | //Exp9// ``>=`` //Exp10//
  |  |  **|**  | //Exp10//
  | //Exp8// | -> | //Exp8// ``==`` //Exp9//
  |  |  **|**  | //Exp8// ``!=`` //Exp9//
  |  |  **|**  | //Exp9//
  | //Exp4// | -> | //Exp4// ``&&`` //Exp5//
  |  |  **|**  | //Exp5//
  | //Exp3// | -> | //Exp3// ``||`` //Exp4//
  |  |  **|**  | //Exp4//
  | //Exp2// | -> | //Exp3// ``=`` //Exp2//
  |  |  **|**  | //Exp3// ``+=`` //Exp2//
  |  |  **|**  | //Exp3// ``-=`` //Exp2//
  |  |  **|**  | //Exp3// ``?`` //Exp// ``:`` //Exp2//
  |  |  **|**  | //Exp3//
  | //Exp1// | -> | ``throw`` //Exp2//
  |  |  **|**  | //Exp2//
  | //Exp// | -> | //Exp1//
  | //Exp5// | -> | //Exp6//
  | //Exp6// | -> | //Exp7//
  | //Exp7// | -> | //Exp8//
  | //[Exp]// | -> | **eps**
  |  |  **|**  | //Exp//
  |  |  **|**  | //Exp// ``,`` //[Exp]//
  | //ConstantName// | -> | //Id//
  | //[ConstantName]// | -> | //ConstantName//
  |  |  **|**  | //ConstantName// ``::`` //[ConstantName]//
  | //Type// | -> | ``bool``
  |  |  **|**  | ``double``
  |  |  **|**  | ``int``
  |  |  **|**  | ``void``
  |  |  **|**  | ``string``
  |  |  **|**  | //[ConstantName]//
  |  |  **|**  | //Type// ``[]``
  |  |  **|**  | //Type// ``&``
  |  |  **|**  | ``const`` //Type//
  | //[Type]// | -> | //Type//
  |  |  **|**  | //Type// ``,`` //[Type]//
  | //MaybeAss// | -> | ``=`` //Exp//
  |  |  **|**  | **eps**



%% File generated by the BNF Converter (bnfc 2.9.5).
